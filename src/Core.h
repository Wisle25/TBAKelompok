#pragma once

#include <memory>
#include <cmath>
#include <stack>

#include <iostream> // Remove later at release

#include "SFML/Graphics.hpp"
#include "Core/Delegate.h"

// GLOBAL VAR
const sf::Color COLOR = sf::Color(237, 148, 85);
const sf::Color SECONDARY_COLOR = sf::Color(255, 187, 112);
const sf::Color THIRD_COLOR = sf::Color(255, 236, 158);